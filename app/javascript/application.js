// Entry point for the build script in your package.json
import "@hotwired/turbo-rails";
import "./controllers";
import "bootstrap";
import "@rails/actioncable";

// old yarn way, we had to yarn build --watch which ran a JS server
